---
import ClientSPALayout from "../../../layouts/ClientSPALayout.astro";
import { Router } from "../../../components/react/Router";

export const routes = ['c', 'd']

export const prerender = true
---
<ClientSPALayout title="Routing Root" filePath={Astro.self.moduleId!}>
  <Router baseUrl="/client-spa" routes={routes} client:only="react" />
</ClientSPALayout>
